var db = {};
// to do: db connection as per below sequelize example
/*
var sequelize = new Sequelize(
  DATABASE_NAME,
  DATABASE_USERNAME,
  DATABASE_PASSWORD,
  HOST,
);
db.sequelize = sequelize;
*/
export default db;